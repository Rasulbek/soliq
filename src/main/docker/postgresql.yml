version: '2'
services:
  soliq-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/soliq/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=soliq
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
